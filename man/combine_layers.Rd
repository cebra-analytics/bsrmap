% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_layers.R
\name{combine_layers}
\alias{combine_layers}
\alias{combine_layers.Raster}
\alias{combine_layers.SpatRaster}
\title{Combine spatial layers}
\usage{
combine_layers(
  x,
  use_fun = c("prod", "sum", "union"),
  weights = NULL,
  na.rm = FALSE,
  binarize = FALSE,
  filename = "",
  ...
)

\method{combine_layers}{Raster}(
  x,
  use_fun = c("prod", "sum", "union"),
  weights = NULL,
  na.rm = FALSE,
  binarize = FALSE,
  filename = "",
  ...
)

\method{combine_layers}{SpatRaster}(
  x,
  use_fun = c("prod", "sum", "union"),
  weights = NULL,
  na.rm = FALSE,
  binarize = FALSE,
  filename = "",
  ...
)
}
\arguments{
\item{x}{A multi-layer \code{raster::Raster*} or \code{terra::SpatRaster}
object representing the spatial layers to combine.}

\item{use_fun}{One of \code{"prod"}, \code{"sum"}, or \code{"union"}. The
union function is intended for probabilities (via \code{1 - sum(1 - x)}).}

\item{weights}{Optional (default is none) numeric vector of weights for
each layer to be combined via \code{"prod"} or \code{"sum"} only.}

\item{na.rm}{Logical indicating whether or not to ignore \code{"NA"} values
when applying the combination function (\code{"use_fun"}). Default =
\code{FALSE}.}

\item{binarize}{Logical indicating if the combined cells should be
binarized, i.e. set to 1 for values > 0. Default = \code{FALSE}.}

\item{filename}{Optional file writing path (character).}

\item{...}{Additional parameters (passed to \code{writeRaster}).}
}
\value{
A \code{terra::SpatRaster} object containing the combined layer.
}
\description{
Combines multiple spatial layers via (optionally weighted) cell-wise
multiplication, addition, or union (via complements), and optionally
binarizes the output.
}
\note{
Generalized modified version of corresponding layer-combining
functions in \code{\href{edmaps}{https://github.com/jscamac/edmaps}}.
}
\references{
Camac, J. & Baumgartner, J. (2021). \emph{edmaps} (early
detection maps) : An R package for creating Australian maps of
establishment likelihood for terrestrial plant pests.
\url{https://github.com/jscamac/edmaps}.
}
